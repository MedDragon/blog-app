# Blog-App (Laravel + Vue.js + Docker)

## 📌 Про проект
Цей веб-додаток розроблено як базу для вивчення дисципліни **"Автоматизація тестування ПЗ"**. Додаток представляє собою функціональний блог, архітектура якого адаптована під бізнес-логіку інтеграції з **Zoho CRM**.

## 🛠 Технологічний стек
* **Backend:** PHP 8.4 + Laravel 11
* **Frontend:** Vue.js 3 (Composition API) + Inertia.js
* **Database:** MySQL 8.4
* **Infrastructure:** Docker (Laravel Sail)
* **Styling:** Tailwind CSS

## 🚀 Реалізований функціонал (User Stories)
- [x] **Автентифікація:** Повна реєстрація, логін та функціонал скидання пароля (Laravel Breeze).
- [x] **Публікації:** Створення постів із вказанням джерела (Source) та автоматичною генерацією Zoho External ID.
- [x] **Коментарі:** Можливість залишати коментарі до будь-якого посту.
- [x] **Профайл:** Сторінка налаштувань користувача.
- [x] **Seeders:** Автоматичне наповнення бази даних (50 користувачів, 100 постів).

## 📦 Інструкція з розгортання

### 1. Підготовка середовища
Переконайтеся, що у вас встановлені **Docker** та **Docker Compose**.

### 2. Встановлення проекту
Клонуйте репозиторій та перейдіть у папку:
```bash
git clone [посилання-на-ваш-репозиторій]
cd blog-app
```

### 3. Запуск контейнерів
```bash
composer install
./vendor/bin/sail up -d
```

### 4. Налаштування бази даних
```bash
sail php artisan migrate:fresh --seed
```

### 5. Компіляція фронтенду
```bash
sail npm install
sail npm run dev
```

**Тестові дані для входу:**
* **Email:** test@example.com
* **Password:** 1

## 🧪 Тестування
Проект підготовлений для автоматизації тестування:
* **Unit-тести:** Для логіки розподілу менеджерів залежно від source.
* **Feature-тести:** Для перевірки зв'язків між User, Post та Comment.
